name = "referral"
main = "worker.js"
compatibility_date = "2026-08-01"

# Nightly sweep: pending commissions past the refund window become approved.
# Without this nothing ever leaves 'pending' and every partner dashboard shows
# nothing owed, forever.
[triggers]
crons = ["0 3 * * *"]
