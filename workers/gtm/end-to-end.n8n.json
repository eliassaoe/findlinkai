{
  "name": "GTM end to end \u2014 Explee companies to Instantly",
  "nodes": [
    {
      "parameters": {},
      "id": "53f06719-c18e-46b6-9bfd-8ca68528db3b",
      "name": "Start",
      "type": "n8n-nodes-base.manualTrigger",
      "typeVersion": 1,
      "position": [
        -40,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// Everything from the console. Edit here and the whole flow follows.\nreturn [{ json: {\n  \"explee_key\": \"PUT_EXPLEE_KEY_HERE\",\n  \"instantly_key\": \"PUT_INSTANTLY_KEY_HERE\",\n  \"openrouter_key\": \"PUT_OPENROUTER_KEY_HERE\",\n  \"linkfinder_key\": \"PUT_LINKFINDER_KEY_HERE\",\n  \"linkfinder_max\": 25,\n  \"linkfinder_budget_seconds\": 240,\n  \"linkfinder_concurrency\": 3,\n  \"model\": \"anthropic/claude-opus-5\",\n  \"instantly_campaign_id\": \"PUT_INSTANTLY_CAMPAIGN_ID_HERE\",\n  \"page_size\": 25,\n  \"domains\": [],\n  \"seniority\": \"director\",\n  \"per_company\": 5,\n  \"explee_companies_path\": \"/public/api/v1/search/companies\",\n  \"job_titles\": [\n    \"Dirigeant\",\n    \"responsable commercial\"\n  ],\n  \"definition\": \"organisme de formation, centre de formation in France, Belgique, Luxembourg\",\n  \"criteria\": [\n    \"Vente B2B active\",\n    \"Equipe commerciale de moins de 5\",\n    \"is NOT: Formation purement subventionnee\"\n  ],\n  \"offer\": \"Outbound run end to end, paid per meeting held.\",\n  \"problem\": \"Prospecting stops whenever the team is delivering training, so the pipeline arrives in waves.\",\n  \"facts\": \"LinkFinder AI turns a name, company, email or LinkedIn URL into a verified business email or phone number. Plans start at $49/month for 5,000 credits. A LinkedIn-profile-to-email lookup costs 10 credits.\\n\\nThe outbound service: we build the list, write the campaign, run the sending and handle replies. You pay per meeting held.\",\n  \"instructions\": \"Ask for a reply, not a call. Never mention a discount. Name the pay-per-meeting model once \u2014 it is the whole reason this offer is different \u2014 and do not explain it twice.\",\n  \"language\": \"lead\"\n} }];"
      },
      "id": "499bcda4-20d0-45c6-9aea-3b3db3937d7d",
      "name": "Config",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        180,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "={{ 'https://api.explee.com' + $json.explee_companies_path }}",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "X-API-Key",
              "value": "={{ $json.explee_key }}"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ filters: { definition: $json.definition, criteria: ($json.criteria || []).slice(0, 3) }, page_size: $json.page_size }) }}",
        "options": {}
      },
      "id": "8b2e3f5a-0c40-46a5-995e-8ac2b49832bc",
      "name": "Explee: search companies",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        400,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// The one job here is to end up with a list of domains, and to say\n// exactly what came back when it cannot. Silent empties are what made\n// the last three attempts impossible to debug.\nconst cfg = $('Config').first().json;\nif (cfg.domains && cfg.domains.length) {\n  return cfg.domains.map(domain => ({ json: { domain } }));\n}\nconst j = $input.first().json;\nconst rows = j.companies || j.results || j.items || j.data || [];\nconst clean = d => String(d || '').trim().toLowerCase()\n  .replace(/^https?:\\/\\//, '').replace(/^www\\./, '').replace(/\\/.*$/, '');\nconst domains = [...new Set(rows\n  .map(r => clean(r.domain || r.website || r.company_domain || r.companyWebsite ||\n                  r.company_website || r.url))\n  .filter(d => d.includes('.')))];\nif (!domains.length) {\n  throw new Error('No company domains in the Explee response. Top-level keys: ' +\n    Object.keys(j).join(', ') + '. First row: ' +\n    JSON.stringify(rows[0] || j).slice(0, 400));\n}\nconsole.log(domains.length + ' companies: ' + domains.join(', '));\nreturn domains.map(domain => ({ json: { domain } }));"
      },
      "id": "3f7780c4-e672-4b28-87bd-d8448f22280e",
      "name": "Domains",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        620,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// Explee first, LinkFinder second.\n//\n// Explee matches job titles semantically against the campaign's target\n// roles, so it answers \"who at this company is my buyer\". LinkFinder's\n// company_domain_to_employees takes a seniority bucket instead: a live\n// call on demos.fr returned a Technical Director based in Russia and a\n// CFO \u2014 real directors, not the people this campaign is for.\n//\n// The trade is cost. Explee returns profiles without addresses at 1\n// credit each, so every one of them goes through email resolution\n// downstream. LinkFinder bundles the email into its 1 credit when it\n// has one. Precision is worth more than that on cold email.\nconst cfg = $('Config').first().json;\nif (!this.helpers || !this.helpers.httpRequest) {\n  throw new Error('This n8n has no this.helpers.httpRequest in Code nodes.');\n}\nconst http = opts => this.helpers.httpRequest(opts);\nconst sleep = ms => new Promise(r => setTimeout(r, ms));\nconst LF = 'https://api.linkfinderai.com';\nconst hasExplee = cfg.explee_key && !cfg.explee_key.startsWith('PUT_');\nconst hasLf = cfg.linkfinder_key && !cfg.linkfinder_key.startsWith('PUT_');\nif (!hasExplee && !hasLf) throw new Error('No Explee or LinkFinder key in Config.');\n\n// Explee: the roles this campaign actually wants, matched semantically.\nasync function explee(domain) {\n  if (!hasExplee) return [];\n  try {\n    const x = await http({ method: 'POST', json: true,\n      url: 'https://api.explee.com/public/api/v1/search/people-by-domains',\n      headers: { 'X-API-Key': cfg.explee_key },\n      body: { domains: [domain],\n              job_titles: (cfg.job_titles || ['Founder', 'Director']).slice(0, 20),\n              people_per_company: cfg.per_company } });\n    return (x && (x.people || x.results || x.items)) || [];\n  } catch (e) { console.log(domain + ' explee: ' + e.message); return []; }\n}\n\n// LinkFinder: always async, so post then poll the job inline. A Code\n// node rather than a Wait/If loop, because that loop is what hung the\n// first two versions of this flow.\nasync function linkfinder(domain) {\n  if (!hasLf) return [];\n  const auth = { Authorization: 'Bearer ' + cfg.linkfinder_key };\n  let r;\n  try {\n    r = await http({ method: 'POST', url: LF, headers: auth, json: true, body: {\n      type: 'company_domain_to_employees', input_data: domain,\n      seniority: cfg.seniority, employee_count: cfg.per_company } });\n  } catch (e) { console.log(domain + ' linkfinder: ' + e.message); return []; }\n  for (let n = 0; r && r.job_id && !r.result && n < 8; n++) {\n    await sleep(8000);\n    try { r = await http({ method: 'GET', headers: auth, json: true,\n      url: r.poll_url || LF + '/status/' + r.job_id }); }\n    catch (e) { console.log(domain + ' poll: ' + e.message); return []; }\n  }\n  return (r && r.result) || [];\n}\n\nconst out = [];\nfor (const { json: { domain } } of $input.all()) {\n  let rows = await explee(domain);\n  let via = 'explee';\n  if (!rows.length) { rows = await linkfinder(domain); via = 'linkfinder'; }\n  console.log(domain + ': ' + rows.length + ' from ' + (rows.length ? via : 'neither'));\n  for (const p of rows.slice(0, cfg.per_company)) out.push({ ...p, source: via });\n  await sleep(1100);\n}\nif (!out.length) {\n  throw new Error('Neither Explee nor LinkFinder found anyone at: ' +\n    $input.all().map(i => i.json.domain).join(', '));\n}\nreturn [{ json: { people: out } }];"
      },
      "id": "e748480e-f761-4bae-a785-f9e189b4b799",
      "name": "People at each domain",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        840,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// One item holding every person. Fan them out and normalise the two\n// fields the rest of the flow reads. Nobody is dropped here \u2014 search\n// returns profiles, LinkFinder turns them into addresses next.\nconst j = $input.first().json;\nconst contacts = j.people || j.results || j.items || j.contacts || [];\n// Explee answers in snake_case, LinkFinder in camelCase. Read both.\nif (!contacts.length) {\n  throw new Error('Explee returned no people. It answered with: ' +\n    JSON.stringify(j).slice(0, 400));\n}\nreturn contacts.map(c => ({ json: {\n  ...c,\n  email: c.email || '',\n  linkedin_url: c.linkedin_url || c.linkedinUrl || c.linkedin || '',\n  full_name: c.full_name || c.name ||\n    [c.firstName, c.lastName].filter(Boolean).join(' '),\n  first_name: c.first_name || c.firstName || '',\n  job_title: c.job_title || c.jobTitle || c.title || '',\n  company_name: c.company_name || c.company || '',\n  company_domain: c.company_domain || c.companyWebsite || c.domain || '',\n  location: c.location || c.city || c.country || '',\n  // What the writer is allowed to notice. The raw row also carries NACE\n  // sector scores, follower counts, photo urls and a company_geo that\n  // said CN for Microsoft \u2014 noise that dilutes the signal and invites\n  // the model to invent from it.\n  brief: Object.fromEntries(Object.entries({\n    name: c.full_name || c.name ||\n      [c.firstName || c.first_name, c.lastName || c.last_name].filter(Boolean).join(' '),\n    title: c.job_title || c.jobTitle || c.title,\n    headline: c.headline,\n    company: c.company_name || c.company,\n    what_the_company_does: c.company_description,\n    industry: c.company_industry || c.industry,\n    company_size: c.company_size,\n    where: c.city || c.location || c.geo || c.country,\n  }).filter(([, v]) => v)),\n} }));"
      },
      "id": "3e65bed3-f0ab-485f-9149-0a637033bf18",
      "name": "One item per lead",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1060,
        300
      ]
    },
    {
      "parameters": {
        "jsCode": "// Explee returns profiles, not addresses. This is what turns them into leads:\n// 10 credits from a LinkedIn URL, 7 from name + company, charged whether or\n// not one comes back.\n//\n// n8n kills a Code node at 300s (N8N_RUNNERS_TASK_TIMEOUT). A serial loop over\n// 47 leads with a 1.1s spacer and an occasional 8s job poll blows that easily,\n// so this works to a wall-clock budget with a few lookups in flight at once,\n// and returns what it has when the budget runs out rather than dying with\n// nothing. Raising the cap or the concurrency past the plan's requests/second\n// is how you trade credits for 429s \u2014 the defaults sit under Starter's 5/s.\nconst cfg = $('Config').first().json;\nconst leads = $input.all().map(i => i.json);\nconst canHttp = !!(this.helpers && this.helpers.httpRequest);\nconst http = opts => this.helpers.httpRequest(opts);\nconst sendable = () => leads.filter(l => l.email).map(json => ({ json }));\n\nif (!canHttp || !cfg.linkfinder_key || cfg.linkfinder_key.startsWith('PUT_')) {\n  return sendable();\n}\n\nconst auth = { Authorization: 'Bearer ' + cfg.linkfinder_key };\nconst API = 'https://api.linkfinderai.com';\nconst sleep = ms => new Promise(r => setTimeout(r, ms));\nconst deadline = Date.now() + (Number(cfg.linkfinder_budget_seconds) || 240) * 1000;\nconst left = () => deadline - Date.now();\n\n// Only the leads that need a lookup and give us something to look up by.\nconst queue = [];\nfor (const lead of leads) {\n  if (lead.email) continue;\n  const op = lead.linkedin_url\n    ? { type: 'linkedin_profile_to_email', input_data: lead.linkedin_url }\n    : { type: 'lead_full_name_to_email', input_data:\n        [lead.full_name || lead.name, lead.company_name || lead.company]\n          .filter(Boolean).join(' ') };\n  if (!op.input_data) continue;\n  queue.push({ lead, op });\n}\nconst budget = queue.splice(Number(cfg.linkfinder_max) || 0);\nconst skippedByCap = budget.length;\n\nlet found = 0, spent = 0, stopped = '';\nlet next = 0;\n\nasync function worker() {\n  while (next < queue.length && !stopped) {\n    if (left() < 15000) { stopped = 'out of time'; break; }\n    const { lead, op } = queue[next++];\n    spent += op.type === 'linkedin_profile_to_email' ? 10 : 7;\n    let r;\n    try {\n      r = await http({ method: 'POST', url: API, headers: auth, body: op,\n                       json: true, timeout: Math.min(30000, left()) });\n    } catch (e) {\n      // 402 out of credits, 401 bad key, 429 too fast: stop, do not hammer.\n      stopped = e.message;\n      break;\n    }\n    // Any endpoint can hand back a job instead of a result if it runs long.\n    // Poll only while there is budget to spare for it.\n    for (let n = 0; r && r.job_id && !r.result && n < 3 && left() > 20000; n++) {\n      await sleep(5000);\n      try {\n        r = await http({ method: 'GET', headers: auth, json: true,\n                         url: r.poll_url || API + '/status/' + r.job_id });\n      } catch (e) { r = null; }\n    }\n    const res = r && r.result;\n    const email = typeof res === 'string' ? res : (res && res.email) || '';\n    if (email) { lead.email = email; found++; }\n    await sleep(1100);          // per worker, so ~concurrency requests/second\n  }\n}\n\nconst lanes = Math.max(1, Math.min(Number(cfg.linkfinder_concurrency) || 3, 5));\nawait Promise.all(Array.from({ length: lanes }, worker));\n\nconst done = sendable();\nconsole.log('LinkFinder: ' + found + ' emails, ~' + spent + ' credits, ' +\n  done.length + ' of ' + leads.length + ' sendable' +\n  (skippedByCap ? '; ' + skippedByCap + ' past linkfinder_max' : '') +\n  (stopped ? '; stopped early: ' + stopped : ''));\nreturn done;"
      },
      "id": "a89c1319-c168-4ee4-a42b-5524ac3fd8e1",
      "name": "LinkFinder: fill missing emails",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1280,
        300
      ]
    },
    {
      "parameters": {
        "promptType": "define",
        "text": "={{ 'Write one cold email to this person.\\n\\n' + JSON.stringify($json.brief, null, 2) }}",
        "options": {
          "systemMessage": "You write one cold email to one person. It is the only email they will\nget from us this week, and it has to earn a reply on its own.\n\n## WHO YOU ARE WRITING AS\nlinkfinderai.com\n\n## WHAT WE SELL\nOutbound run end to end, paid per meeting held.\n\n## THE ONLY THINGS YOU MAY STATE AS TRUE\nEverything below, and nothing else. If a claim is not here, it does not\nexist. Never invent a metric, a client, a mutual connection, a funding\nround or an event. A shorter email is always better than an invented one.\n\nLinkFinder AI turns a name, company, email or LinkedIn URL into a verified business email or phone number. Plans start at $49/month for 5,000 credits. A LinkedIn-profile-to-email lookup costs 10 credits.\n\nThe outbound service: we build the list, write the campaign, run the sending and handle replies. You pay per meeting held.\n\nBooking link (use ONLY if the instructions below say to): https://calendly.com/hamoureliasse/offre-linkfinder-ai-outbound/\n\nClients we may name:\n- Cegos\n- Demos\n- Orsys\n\n## WHO YOU ARE WRITING TO\nRole: Dirigeant, responsable commercial\nCompany: organisme de formation, centre de formation in France, Belgique, Luxembourg\nThey typically:\n- Vente B2B active\n- Equipe commerciale de moins de 5\n\nThey are not:\n- Formation purement subventionnee\n- Plus de 200 salaries\n\nThe problem they likely have:\nProspecting stops whenever the team is delivering training, so the pipeline arrives in waves.\n\n## HOW TO WRITE IT\nFour moves, in this order, nothing else:\n1. An observation. One concrete thing from the lead brief you are given \u2014\n   what their company does, their role, where they are. Not a compliment,\n   not \"I came across your profile\". This is the whole difference between\n   a 3% reply rate and a 15% one, so make it specific or leave it out.\n2. The consequence. Why that likely creates the problem above. One line.\n3. What we do. Concrete and operational, in the order it happens.\n4. One ask. A question they can answer in a line. Ask for a reply, not a\n   call \u2014 the lowest-commitment ask wins the first touch. Never two asks.\n\nLength: 75-100 words. Never over 125. Under 200 words replies 2.4x better\nthan over, and shorter still wins inside that. Short paragraphs, one to\ntwo lines each, blank line between.\n\nSubject: four to seven words, lowercase, curiosity plus relevance. It\nshould read like a person typed it to one person. No clickbait, no\n\"quick question\", no company name padding.\n\nLanguage: write in the language of their country. If unclear, English.\nWrite as a native speaker writes business email \u2014 not translated English.\n\n## NEVER\n- \"I hope this finds you well\", \"I wanted to reach out\", \"I came across\"\n- \"Quick question\", \"circle back\", \"touch base\", \"synergy\", \"leverage\"\n- \"simply\", \"just\", \"easily\", \"solution\", \"game-changer\"\n- Compliments on their website, growth, or \"impressive work\"\n- A second ask, a P.S., a signature block, or an unsubscribe line\n- Anything you were not told above\n\n## EXTRA INSTRUCTIONS FOR THIS CAMPAIGN\nAsk for a reply, not a call. Never mention a discount. Name the pay-per-meeting model once \u2014 it is the whole reason this offer is different \u2014 and do not explain it twice.\n\n## OUTPUT\nReply with JSON and nothing else, no code fence:\n{\"subject\": \"...\", \"body\": \"...\"}"
        }
      },
      "id": "0d01c7b2-aeae-4a26-a4ed-ece3057b10bd",
      "name": "Write the email",
      "type": "@n8n/n8n-nodes-langchain.agent",
      "typeVersion": 3.1,
      "position": [
        1500,
        300
      ]
    },
    {
      "parameters": {
        "model": "={{ $('Config').first().json.model }}",
        "options": {}
      },
      "id": "a643d0e6-bb2e-4e21-ac0b-dad0a1905089",
      "name": "Model",
      "type": "@n8n/n8n-nodes-langchain.lmChatOpenAi",
      "typeVersion": 1.1,
      "position": [
        1500,
        480
      ]
    },
    {
      "parameters": {
        "jsCode": "// Parse the model's JSON and shape one Instantly lead. The per-lead copy\n// rides as custom variables, so one campaign sends a different email to\n// everyone in it.\nconst out = [];\nfor (let i = 0; i < $input.all().length; i++) {\n  const reply = $input.all()[i].json;\n  const lead = $('LinkFinder: fill missing emails').all()[i].json;\n  // The agent answers in .output; a raw chat completion in .choices.\n  let text = reply.output ?? reply.choices?.[0]?.message?.content ?? '';\n  if (typeof text !== 'string') text = JSON.stringify(text);\n  if (text.startsWith('```')) text = text.split('```')[1].replace(/^json/, '').trim();\n  let email;\n  try { email = JSON.parse(text); }\n  catch (e) { continue; }   // model did not return JSON \u2014 skip, do not send junk\n  const full = lead.full_name || lead.name || '';\n  out.push({ json: {\n    email: lead.email,\n    first_name: lead.first_name || full.split(' ')[0] || '',\n    last_name: full.split(' ').slice(1).join(' '),\n    company_name: lead.company_name || lead.company || '',\n    custom_variables: {\n      subject: email.subject,\n      body: String(email.body).replace(/\\r\\n/g, '\\n').replace(/\\n/g, '<br>'),\n    },\n  } });\n}\nif (!out.length) throw new Error('Nothing to send \u2014 the writer returned no usable JSON.');\n// Fail here rather than at the Instantly call, so the drafted emails\n// above are visible and only the destination is missing.\nconst camp = $('Config').first().json.instantly_campaign_id;\nif (!camp || camp.startsWith('PUT_')) {\n  throw new Error(out.length + ' emails are written and ready. Put an Instantly ' +\n    'campaign id in the Config node to add them \u2014 the id is in the campaign URL.');\n}\nconsole.log(out.length + ' leads ready for campaign ' + camp);\nreturn out;"
      },
      "id": "9b6768f8-fef8-4b96-8413-0354f7546730",
      "name": "Build the lead",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [
        1720,
        300
      ]
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.instantly.ai/api/v2/leads/list",
        "sendHeaders": true,
        "headerParameters": {
          "parameters": [
            {
              "name": "Authorization",
              "value": "={{ 'Bearer ' + $('Config').first().json.instantly_key }}"
            },
            {
              "name": "Content-Type",
              "value": "application/json"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({ campaign_id: $('Config').first().json.instantly_campaign_id, skip_if_in_campaign: true, skip_if_in_workspace: true, leads: $input.all().map(i => i.json) }) }}",
        "options": {}
      },
      "id": "e39acbfa-669b-46fd-88c7-e175de76eab6",
      "name": "Instantly: add leads",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [
        1940,
        300
      ]
    }
  ],
  "connections": {
    "Start": {
      "main": [
        [
          {
            "node": "Config",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Config": {
      "main": [
        [
          {
            "node": "Explee: search companies",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Explee: search companies": {
      "main": [
        [
          {
            "node": "Domains",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Domains": {
      "main": [
        [
          {
            "node": "People at each domain",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "People at each domain": {
      "main": [
        [
          {
            "node": "One item per lead",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "One item per lead": {
      "main": [
        [
          {
            "node": "LinkFinder: fill missing emails",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "LinkFinder: fill missing emails": {
      "main": [
        [
          {
            "node": "Write the email",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Write the email": {
      "main": [
        [
          {
            "node": "Build the lead",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Build the lead": {
      "main": [
        [
          {
            "node": "Instantly: add leads",
            "type": "main",
            "index": 0
          }
        ]
      ]
    },
    "Model": {
      "ai_languageModel": [
        [
          {
            "node": "Write the email",
            "type": "ai_languageModel",
            "index": 0
          }
        ]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "pinData": {}
}