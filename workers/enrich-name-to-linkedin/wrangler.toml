name = "enrich-name-to-linkedin"
main = "worker.js"
compatibility_date = "2026-09-12"

# Secrets, set with `wrangler secret put`, never committed:
#   SUPABASE_URL          - https://snxhsboboatjywgwdeds.supabase.co
#   SUPABASE_SERVICE_KEY  - service_role key (this worker writes credits)
#   SERPER_API_KEY        - from serper.dev
#
# The Serper key is currently hardcoded inside the n8n workflow JSON, which means
# it travels in every export of that workflow. Rotate it when this ships and put
# the new one here only.
