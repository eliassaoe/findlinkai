name = "dodo-posthog-webhook"
main = "worker.js"
compatibility_date = "2026-08-20"

# Secrets are set with `wrangler secret put`, never committed:
#   DODO_WEBHOOK_SECRET  - Dodo dashboard -> Webhooks -> signing secret (whsec_...)
#   POSTHOG_API_KEY      - PostHog project API key (phc_...)
