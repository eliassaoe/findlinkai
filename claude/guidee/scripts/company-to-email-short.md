# Turn A Company Name Into A Contact Email With LinkFinder AI

| | |
| --- | --- |
| **Parent video** | `company-to-email` — `scripts/company-to-email.md` (storyboard complete, not yet recorded) |
| **Beat used** | steps 08-10 of the parent's click script — type the company name, click Enrich Data, the email appears. The bulk-CSV half of the parent video is not used here. |
| **Runtime** | 25s |
| **Demo data** | identical to the parent: `Salesforce` |
| **Title score** | not yet scored — `vidiq_score_title` (`type: short`) was out of credits when this was written. Score before recording. |

## Cold open (0-2s)

> [Result panel already on screen: a contact email next to "Salesforce". Held for two seconds before any narration.]

## Narration

> That's a real contact email, from just a company name. No LinkedIn profile, no guessing at name@domain.com.
>
> [cut to the input] Type the company name, click Enrich Data —
>
> [cut back to the result] — and it comes back tied to their own domain. Five credits. Works for any company, not just the big ones.

## Caption overlay

- Input: `Salesforce`
- Result: contact email (blur the local part on screen, keep the domain visible)

## Reframe notes

Source is the parent's step 08-10 footage at 1727x1080 (LinkFinder AI dashboard). Crop tight on the input field and result panel, same treatment as the other dashboard-based Shorts in this batch. Blur or partially mask the returned email's local part before publishing — showing a real inbox address on a public Short is unnecessary exposure the long-form video doesn't need to avoid (it's a slower, less clip-able format) but a Short racking up views should.
