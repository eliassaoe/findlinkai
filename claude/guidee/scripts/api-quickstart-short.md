# One API Call Gets You A Company's Website With LinkFinder AI

| | |
| --- | --- |
| **Parent video** | `api-quickstart` — `scripts/api-quickstart.md` (storyboard complete, not yet recorded) |
| **Beat used** | steps 08-10 of the parent's click script — type the curl command, press Enter, read the JSON response |
| **Runtime** | 28s |
| **Demo data** | identical to the parent: `Tesla` -> `tesla.com` |
| **Title score** | not yet scored — `vidiq_score_title` (`type: short`) was out of credits when this was written. Score before recording. |

## Cold open (0-2s)

> [Terminal already on screen, JSON response visible: `{"result": "tesla.com", "status": "success"}`. No narration yet — just the result, held for two seconds.]

## Narration

> That's one API call. No dashboard, no login screen — just this.
>
> [cut to the request] Here's the whole thing: one endpoint, your API key in the header, and a JSON body telling it what you want — company name in, website out.
>
> [cut back to the result] Press enter, and that comes back in under a second. This one costs one credit. Full docs are in the link below.

## Caption overlay

- Input: `"input_data": "Tesla"`
- Result: `"result": "tesla.com"`

## Reframe notes

Source is the parent's step 08-10 footage at 1727x1080 (terminal only, no browser chrome visible in that beat). Punch in tight on the terminal window, centered — it's already close to a single-column shape, so this is a straightforward crop to 1080x1920 with the terminal filling nearly the full vertical frame, not a re-record.
