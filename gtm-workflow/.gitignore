# The learning memory is per-deployment state, not project content.
campaign_memory.db
campaign_memory.db-wal
campaign_memory.db-shm

# Real credentials never belong in the repo — .env.example is the committed one.
.env
