{
  "name": "gtm-signal-engine",
  "version": "1.0.0",
  "private": true,
  "description": "Self-learning signal-driven GTM workflow for n8n: Trigify -> Findymail -> Claude -> Deliveryman, with a SQLite conversion memory that feeds back into qualification.",
  "engines": { "node": ">=22.5.0" },
  "main": "src/server.js",
  "scripts": {
    "init": "node src/init-db.js",
    "init:seed": "node src/init-db.js --seed",
    "reset": "node src/init-db.js --reset",
    "show": "node src/init-db.js --show",
    "start": "node src/server.js",
    "test": "node --test test/*.test.js"
  },
  "dependencies": {},
  "license": "UNLICENSED"
}
