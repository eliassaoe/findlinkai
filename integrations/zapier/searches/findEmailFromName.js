'use strict';

// GENERATED by build.mjs from integrations/catalog/operations.json. Do not edit.
//
// Find Email from Name — 7 credits.

const { runEnrichment } = require('../lib/linkfinder');

const OPERATION = {
  "type": "lead_full_name_to_email",
  "inputLabel": "Full Name and Company",
  "params": [],
  "outputField": "email",
  "altType": null,
  "compositeInput": {
    "parts": [
      {
        "name": "name",
        "label": "Full Name",
        "required": true
      },
      {
        "name": "company",
        "label": "Company",
        "required": false
      },
      {
        "name": "location",
        "label": "Location",
        "required": false
      },
      {
        "name": "job_title",
        "label": "Job Title",
        "required": false
      }
    ],
    "joinWith": " "
  }
};

module.exports = {
  key: 'findEmailFromName',
  noun: "Email",

  display: {
    label: "Find Email from Name",
    description: "Find Email from Name using LinkFinder AI. Costs 7 credits per lookup, including lookups that find nothing.",
  },

  operation: {
    inputFields: [
      {
            "key": "name",
            "label": "Full Name",
            "type": "string",
            "required": true,
            "helpText": "The person's full name. \"Doe, John\" is flipped to \"John Doe\" automatically.",
            "placeholder": "Bill Gates"
    },
      {
            "key": "company",
            "label": "Company",
            "type": "string",
            "required": false,
            "helpText": "Where they work. Much the strongest signal after the name. Optional, and free — it costs no extra credits and narrows the match.",
            "placeholder": "Microsoft"
    },
      {
            "key": "location",
            "label": "Location",
            "type": "string",
            "required": false,
            "helpText": "City, region or country. Separates people who share a name. Optional, and free — it costs no extra credits and narrows the match.",
            "placeholder": "Seattle"
    },
      {
            "key": "job_title",
            "label": "Job Title",
            "type": "string",
            "required": false,
            "helpText": "Their role. Helps when a company has several people with the same name. Optional, and free — it costs no extra credits and narrows the match.",
            "placeholder": "Co-chair"
    }
],

    perform: (z, bundle) => runEnrichment(z, bundle, OPERATION),

    sample: {
      "id": "bill.gates@microsoft.com",
      "value": "bill.gates@microsoft.com",
      "email": "bill.gates@microsoft.com"
},

    outputFields: [
      {
            "key": "value",
            "label": "Value"
      },
      {
            "key": "email",
            "label": "Email"
      }
],
  },
};
