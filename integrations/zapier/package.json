{
  "name": "zapier-platform-linkfinderai",
  "version": "1.0.0",
  "description": "LinkFinder AI for Zapier \u2014 turn a name, email, company or LinkedIn URL into verified contact and company data.",
  "main": "index.js",
  "private": true,
  "scripts": {
    "build": "node build.mjs",
    "test": "node --test test/*.test.js",
    "validate": "zapier validate",
    "push": "npm run build && zapier push"
  },
  "dependencies": {
    "zapier-platform-core": "17.6.0"
  },
  "devDependencies": {
    "zapier-platform-cli": "^17.6.0"
  },
  "engines": {
    "node": ">=20",
    "npm": ">=9"
  },
  "overrides": {
    "@types/node": "^22.0.0"
  }
}
